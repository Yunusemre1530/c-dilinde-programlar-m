#include<stdio.h>
struct ogrenci{
	char ad[20];
	char soyad[20];
	int no[10];
	
};
int main(){
	struct ogrenci ogrenci1;
	printf("ogrencinin adýný soyadýný ve no'sunu giriniz. "); 
	gets(ogrenci.ad);
	gets(ogrenci.soyad);
	scanf("%d",&ogrenci.no);
	printf("%s %s %d",ogrenci.ad,ogrenci.soyad,ogrenci.no);
}
